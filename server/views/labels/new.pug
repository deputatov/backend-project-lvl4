extends ../layouts/application.pug

block header
  = t('views.labels.new.title')

block content
  +formFor()(action=route('labels#create') method='post')
    +input(label, 'name', errors)(type="text", autofocus)
    +submit(label)(value=t('views.labels.new.submit'))